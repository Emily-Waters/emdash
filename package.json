{
  "name": "@emilywaters/emdash",
  "version": "1.0.14",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsup": "npx tsup",
    "dev:build": "tsx esbuild.ts --watch",
    "dev:tsc": "tsc --watch --preserveWatchOutput",
    "dev:test": "node --watch dist/__test__ ",
    "dev": "concurrently \"npm run dev:*\"",
    "build": "rm -rf ./dist && tsx esbuild.ts && tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.13.4",
    "concurrently": "^9.1.2",
    "esbuild": "^0.25.0",
    "tsup": "^8.3.6",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "xyz-valid": "^1.0.2"
  }
}
